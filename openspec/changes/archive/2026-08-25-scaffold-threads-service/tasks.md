## 1. Workspace scaffold

- [x] 1.1 Create root `Cargo.toml` (two members: `crates/threads-archive`, `services/threads-archive`), `rust-toolchain.toml` pinning 1.97.0, `clippy.toml` (msrv 1.97, test allowances, disallowed env reads naming `ratatoskr_threads_archive::config`, size limits 100/7/5), `deny.toml`, `rustfmt.toml`, `.gitignore` additions, member manifests with workspace lints and exact-pinned dependencies, and lib/bin skeletons that compile. Verification: `cargo check --workspace --locked` succeeds. This task cannot start from a failing test: it creates configuration and manifest files only.
- [x] 1.2 Add `compose.yaml` (postgres:17 pinned by digest on 127.0.0.1:5437, ICU initdb args, user/database `threads`) and start it; verify `pg_isready -h 127.0.0.1 -p 5437 -U threads` succeeds. This task cannot start from a failing test: local infrastructure provisioning.

## 2. Configuration strictness

- [x] 2.1 Add `crates/threads-archive/tests/config.rs` with tests named for the spec scenarios: `empty_environment_yields_loopback_default_on_port_9084` asserting the default admin bind is `127.0.0.1:9084` with no database URL; `unknown_prefixed_key_is_refused_without_echoing_value` asserting `RATATOSKR__NOT_A_SECTION__VALUE=1` fails and names the key; `multiple_violations_reported_together_value_free` asserting a non-loopback admin bind plus zero connection limit both appear in one error with neither supplied value rendered; `recognized_override_changes_exactly_its_own_field`; `valid_configuration_report_renders_no_secret_material`. Confirm they fail against a skeleton loader that accepts everything. Verification: `cargo test -p ratatoskr-threads-archive --test config --locked` fails on the stated assertions.
- [x] 2.2 Implement the strict closed-key typed loader (`src/config.rs`: defaults, validation collecting every violation, value-free report) until green. Verification: same command green.

## 3. Telemetry bootstrap

- [x] 3.1 Add `crates/threads-archive/tests/telemetry.rs`: `initialization_succeeds_once_and_second_call_is_typed_error` asserting a second init returns the already-installed variant of `TelemetryError`; `emitted_records_parse_as_json_with_identity_fields` asserting records carry service name `ratatoskr-threads`, version, and git SHA. Confirm it fails while initialization is unimplemented. Verification: `cargo test -p ratatoskr-threads-archive --test telemetry --locked` fails to provide the passing behavior.
- [x] 3.2 Implement `src/telemetry.rs` (JSON subscriber via try-init with env filter, Prometheus handle, build-info gauge, typed `TelemetryError`) until green. Verification: same command green.

## 4. Owned schema and disposable-database harness

- [x] 4.1 Add `src/database.rs` (`Database::connect`, `apply_schema` under advisory lock keyed on schema presence, `ping`, `close`, typed `PersistenceError`) and the `test-support` feature exposing `TestDatabase`; add `crates/threads-archive/tests/schema.rs` asserting: `fresh_apply_creates_every_declared_table_and_nothing_else`; `second_apply_is_a_no_op_and_concurrent_applies_both_succeed`; `unknown_acquisition_method_is_refused_by_named_check`; `every_documented_authority_value_inserts_including_explicit_user_capture`; `catalog_shows_zero_cross_schema_foreign_keys`; `two_harness_databases_are_isolated_and_drop_on_cleanup`. Start `schema.sql` at the bare `create schema` statement so the run fails because tables are absent. Verification: `cargo test -p ratatoskr-threads-archive --test schema --locked` fails on missing tables.
- [x] 4.2 Write the first-version `schema.sql`: `threads_archive` tables `accounts`, `credentials`, `posts`, `post_relations`, `media`, `captures`, `capture_resolutions`, `export_runs`, `export_records`, `raw_objects`, `tombstones`, `outbox_events`, `inbox_events` with UUIDv7 primary keys without database defaults, named CHECK vocabularies for status/acquisition (`official_api | share_extension | browser_extension | telegram_capture | data_export | legacy_import`) and saved-authority (`explicit_user_capture | export_observation | unknown`) columns with no authoritative-platform-state value, uniqueness on provider identities where declared, `timestamptz` timestamps, and comments per platform SQL conventions. Verification: same command green.

## 5. Operator routes

- [x] 5.1 Add `services/threads-archive/tests/admin.rs`: `liveness_answers_200_in_starting_ready_and_draining_states`; `readiness_is_503_before_startup_200_after_and_503_while_draining` including a failing database check that carries reason `dependency_unavailable` without flipping readiness; `metrics_returns_prometheus_text_containing_threads_build_info`; `version_returns_name_version_git_sha_and_rust_version`; `every_response_including_unknown_path_carries_no_store`. Confirm it fails against a router serving constant success bodies. Verification: `cargo test -p ratatoskr-threads-archive-service --test admin --locked` fails on the readiness transitions.
- [x] 5.2 Implement the runtime state machine (startup/drain facts plus absent/up/down database probe state) and the admin router until green. Verification: same command green.

## 6. Process boot

- [x] 6.1 Add `services/threads-archive/tests/boot.rs`: spawn the real binary against a disposable database with environment configuration; assert `check_config_exits_0_binding_no_port` and `invalid_configuration_exits_78_with_value_free_report`; assert `ready_reaches_200_after_startup`; assert `live_metrics_version_answer_200_and_unknown_path_404_while_serving`; send SIGTERM and assert `sigterm_exits_0_within_shutdown_bound`. Confirm it fails because the process does not serve yet. Verification: `cargo test -p ratatoskr-threads-archive-service --test boot --locked` fails with readiness not arriving. Observed: 5 of 6 failed exactly so against the refusing stub (`missing_database_url_refuses_startup` passed trivially and stays as the regression guard for 6.2).
- [x] 6.2 Implement `services/threads-archive/src/main.rs`: configuration load with `check-config` mode, telemetry init, database connect and schema application refusing startup on failure, listener bind, readiness marking, periodic database probe feeding the readiness fact, graceful SIGTERM/SIGINT drain within the configured bound exiting 0. Verification: same command green. Observed: 6 passed; 0 failed.

## 7. Gates and documentation

- [x] 7.1 Add `.github/workflows/ci.yml` (postgres service container pinned by digest, pinned actions with `persist-credentials: false`, fetch/deny/fmt/clippy/build/test/doc-test/release/file-ratchet steps plus the ci-versus-DEVELOPMENT.md drift guard) and the matching fenced gate block under "### Rust — also the CI gate" in DEVELOPMENT.md; update DEVELOPMENT.md status text from docs-only bootstrap to the running foundation. This task cannot start from a failing test: CI and documentation artifacts; their consistency check is the drift guard step itself.
- [x] 7.2 Update README.md status blockquote and planned data model list to describe the implemented foundation (service runs locally against PostgreSQL with health endpoints and owned `threads_archive` schema carrying the AGENTS.md table set) while keeping account connection, captures, resolution, imports, and events marked planned; align the planned-model list with AGENTS.md's persistence vocabulary. Update the repository context paragraph in `openspec/config.yaml` that states "This repository holds no code yet". This task cannot start from a failing test: documentation.

## 8. Full gate

- [x] 8.1 Run the complete gate list from DEVELOPMENT.md against a clean tree — `git diff --check`, `openspec validate --all --strict`, `openspec validate --archived`, and every cargo step of ci.yml — and record the results. Verification: every command exits zero. Observed on 2026-08-25: `cargo fetch --locked`; `cargo deny --locked check` (advisories ok, bans ok, licenses ok, sources ok); `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --locked -- -D warnings`; 850-line ratchet; `cargo build --workspace --locked`; `cargo test --workspace --locked` (config 5, schema 6, telemetry 2, admin 5, boot 6, all passing); `cargo test --workspace --locked --doc`; `cargo build --workspace --locked --release`; ci.yml-vs-DEVELOPMENT.md drift guard; `git diff --check`; `openspec validate --all --strict`; `openspec validate --archived` — all exited zero.
